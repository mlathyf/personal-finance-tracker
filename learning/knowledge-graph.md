# Knowledge Graph

The living map of what's actually known, not what's been "covered." This is what future sessions read to decide what to explain, what to quiz, and what to leave alone.

## Status definitions

- **seed** — not yet taught
- **introduced** — explained once (by Claude)
- **practicing** — used it, with help, at least once
- **understood** — explained back in own words *and* passed a check on it

Statuses only move up on real evidence — a direct quote or a described action — never assumed from "we talked about it." A status can be *revised down* if later evidence contradicts it (e.g. a wrong answer on something previously marked understood).

## Quizzing policy

Quiz candidates are anything at **seed**, **introduced**, or **practicing** — that's the point of tracking status. Anything **understood** is skipped as long as it's fresh; re-quiz it only if the last-reviewed date is stale (roughly 2–3 weeks with no use) or new evidence casts doubt on it. Never re-quiz something understood and fresh just because it's convenient.

---

## Low-level (JS fundamentals)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Variables & data types (`let`/`const`, strings, numbers, booleans) | practicing | 2026-09-14 | 2026-09-16 | Declared `const` bindings for all form fields; correctly predicted that `.value` on a `type="number"` and `type="date"` input still returns a string, not a number, and confirmed it live. 2026-09-16: applied `const` to an array meant to be mutated later (`.push` in 5.4) after the reassignment-vs-mutation distinction was explained. Also (task 5.4) chose and correctly used `let nextId = 1;` with `nextId++` for an id counter — first real `let` usage, correctly reasoning it needed reassignment unlike `expenses`. |
| Functions (declaring & calling) | understood | 2026-09-14 | 2026-09-16 | Wrote an arrow function `(e) => {...}` as an event-listener callback, correctly understanding it runs later rather than immediately. Task 5.5: wrote a real named function declaration (`async function fetchExpenses() {...}`), initially forgot to call it, and — when asked to check whether it was called anywhere — correctly answered "no, it's not called anywhere" and added `fetchExpenses();` unaided. Same declare-vs-call distinction now correctly generalized across two different contexts (routes in 5.3, named functions here). |
| Conditionals (`if`/`else`) | seed | — | — | — |
| Loops & iteration (`for`, iterating arrays) | practicing | 2026-09-16 | 2026-09-16 | Task 5.5: wrote `for (let expense of data) {...}` unaided to render one row per fetched expense, correctly using `prepend()` inside the loop to preserve newest-first order. (`const` would've been more precise than `let` there, since `expense` is never reassigned — worth revisiting.) |
| Arrays & array methods (`push`, `filter`, `map`, `reduce`) | practicing | 2026-09-16 | 2026-09-16 | Declared `const expenses = [];` in `server.js` (task 5.2) after discussing why `const` still works for something you'll later `.push()` into. Task 5.4: used `.push(expense)` for real; wrongly predicted `res.json(expenses.push(expense))` would send back the pushed object, then correctly identified after being told that `.push()` returns the array's new *length*, not the item — split it into a separate `.push()` call and `res.json(expense)`. `.filter`/`.map`/`.reduce` still unused. |
| Objects (key-value structure — e.g. one expense record) | practicing | 2026-09-16 | 2026-09-16 | Task 5.4: first wrote `const [amount, date, category, note] = req.body;` (array destructuring on an object — wrong), self-corrected to object destructuring `const { amount, date, category, note } = req.body;` after being asked to check the value's actual shape. Also built a real multi-field object literal (`const expense = { id, amount, date, category, note }`) to push into `expenses`. Task 5.5: destructured `{ id, amount, date, category, note }` from each looped `expense` object correctly, including correctly reasoning that this shadows the outer form-field variables of the same name only inside the loop's block scope. |
| Template literals & string building | practicing | 2026-09-14 | 2026-09-14 | Wrote a template literal combining all four field values into one readable line (`` `Spent MVR ${amount.value} on ${note.value}...` ``) correctly on the first attempt. |
| Floating-point number representation (binary approximation, rounding) | understood | 2026-09-19 | 2026-09-19 | Task 6.4: predicted `19.99 * 100` would give a clean `1999.00`; ran it in the Node REPL and got `1998.9999999999998` instead. After the binary-representation explanation, correctly predicted `4 * 100` would be exact and explained why unaided: "as there is no decimal place after either number" — correctly generalizing the integer-vs-fraction distinction, not just recalling the specific example. |
| DOM selection & manipulation (`querySelector`, creating/updating elements) | practicing | 2026-09-14 | 2026-09-14 | Wrote `document.querySelector` calls for every field; independently switched the form's selector to `#form` for the id-safety reason discussed. Used `document.createElement`, `.textContent` (not `innerHTML`, given `note` is free-text), and `container.prepend()` to build and insert a real element from form data. Correctly chose `form.reset()` over manually clearing each field after a trade-off discussion (single call vs. logic that has to be kept in sync by hand), and correctly predicted the `<select>` would revert to its first option ("Food") rather than go blank. |
| Event listeners (responding to clicks/form submits) | practicing | 2026-09-14 | 2026-09-14 | Wrote `form.addEventListener("submit", (e) => {...})` correctly on the first attempt — passed the callback by reference (didn't fall into the call-it-now-with-parens trap), called `e.preventDefault()`, then correctly predicted and confirmed the page would stay put with a "Submitted" console log. |
| JSON (`stringify`/`parse`) | practicing | 2026-09-16 | 2026-09-16 | Explained (task 5.1) that `express.json()` runs `JSON.parse()` on incoming request bodies. Task 5.3: `res.json(expenses)` runs `JSON.stringify()` under the hood — correctly predicted the `[]` output before confirming. Task 5.6: called `JSON.stringify()` explicitly themselves for the first time, in the `POST` request body — but initially stringified the wrong value (the DOM event `e` instead of the form-data object), self-correcting after being asked what `e` actually represents (recalling `e.preventDefault()` from Section 3 unprompted). |
| Async/await & Promises (needed for `fetch`) | practicing | 2026-09-16 | 2026-09-16 | Task 5.5: after Promises and `async`/`await` were explained with a generic example, wrote `async function fetchExpenses() { const response = await fetch(...); const data = await response.json(); }` correctly, then looped over the resolved data and rendered it — confirmed live with real server data. Debugged two real issues: forgetting to call the function, and a `ReferenceError` from an undeclared variable inside it. Task 5.6: first awaited the *wrong* one of two `fetch` calls (a redundant extra `GET`) while leaving the actual `POST` un-awaited; once asked to compare the two calls, correctly merged the `await ... ; await response.json()` pattern onto the `POST` call itself and removed the redundant one. |
| CSS basics (selectors, box model, layout) | practicing | 2026-09-14 | 2026-09-14 | Wrote `public/style.css` from a plain-language spec with help; independently fixed a `san-serif` typo and generalized `#category` to `select`. Also surfaced and corrected a real misconception: predicted `max-width` on `body` would clip/hide overflowing elements like a fixed-size container — corrected by explaining height is unconstrained by default and nothing clips without an explicit `height` + `overflow` rule, which confirmed live (page just scrolled). |

## Structural (how the pieces fit together)

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Frontend roles: HTML/CSS/JS | understood | 2026-09-13 | 2026-09-13 | "The frontend is what the user will see. It consists of HTML, CSS and JS." |
| Backend/server: Node.js + Express | understood | 2026-09-13 | 2026-09-13 | Explained backend as "the business logic of the project... Node.js with express"; separately justified Express as "popular and simple." |
| API as the frontend↔backend contract | understood | 2026-09-13 | 2026-09-16 | Said APIs "communicate between frontend and backend" — correct at a high level, but needed correction that the API is routes inside the backend code, not a separate technology. **Confirmed 2026-09-16:** built and used the actual contract end-to-end across Section 5 (`GET`/`POST /api/expenses`), and asked precisely the right clarifying question about task 5.6 — whether the flow was "sent, then separately fetched" or one round trip — showing real grasp of request/response as a single exchange, not two disconnected technologies. |
| Client-server request/response cycle (fetch → HTTP → route → response) | understood | 2026-09-14 | 2026-09-16 | Correctly predicted `app.listen`'s effect on process lifetime, then independently reasoned through *why* `curl` showed only `index.html` and not CSS/JS by asking the right question unprompted — after one explanation, correctly predicted and confirmed via two more `curl` calls that each static file is its own separate request/response, not bundled. **Revised down 2026-09-16** during task 5.3 after conflating the browser with the server, then **restored 2026-09-16** in task 5.5: correctly and unprompted stated "the client(browser) is sending the request and the server(backend) is receiving and responding to it," and separately reasoned through why the browser console and terminal are two entirely separate JS runtimes with no shared state, HTTP being the only channel between them. |
| Database: SQLite / relational, tabular data | understood | 2026-09-13 | 2026-09-17 | Justified SQLite as it "requires no separate server to run. I can always migrate to MongoDB or PostgreSQL later if I want." Task 6.1: moved from justification to practice — installed `better-sqlite3`, wrote `new Database("expenses.db")`, correctly predicted this would create a real `.db` file on disk (not just an in-memory handle), and confirmed it appeared in the project folder after running the server. |
| Writing SQL queries (`CREATE TABLE`/`SELECT`/`INSERT`/`DELETE`) | practicing | 2026-09-18 | 2026-09-19 | Task 6.2: wrote a real `CREATE TABLE IF NOT EXISTS expenses (id INTEGER PRIMARY KEY, amount INTEGER, date TEXT, category TEXT, note TEXT)` unaided, after correctly guessing 3 of 4 column types cold (`id`→INTEGER, `category`/`note`→TEXT) and correctly flagging `date` as uncertain rather than guessing wrong. Asked a sharp follow-up on why `.schema` doesn't show `IF NOT EXISTS` in the stored definition, and correctly predicted that running the statement again would change nothing new on disk (no new file, just `expenses.db`'s internal structure) — confirmed via `.schema expenses` output matching exactly, twice. Task 6.3: initially tried `db.all()` (missing `.prepare()`) and, separately, thought the SQL argument to `.prepare()` should be the filename `"expenses.db"` rather than the table name — both corrected after guided questions, converging unaided on the full, correct `db.prepare("SELECT * FROM expenses").all()`. Task 6.4: wrote a full parameterized `INSERT` unaided in SQL-text terms (`VALUES` keyword, correct columns, correct `?` placeholders) on the first real attempt, but needed two rounds of guided debugging on the *JS structure* around it — first wrote `.run(...)` as literal text inside the backtick SQL string alongside `.(?, ?, ?, ?)` instead of `VALUES`, then (after fixing the SQL text) chained `.run(...)` directly onto the SQL string itself rather than onto the object `db.prepare(...)` returns, hitting a real `TypeError: "...".run is not a function` in the terminal. Read that stack trace, correctly predicted "it will do nothing" (wrong — discussed why a thrown error is not "nothing"), then correctly relocated `.run(...)` to chain after `db.prepare(...)`'s closing parenthesis, unaided, on the next try. `DELETE` still unwritten — that's 7.1. |
| Code runs once (module load) vs. once per request (route handler) | practicing | 2026-09-18 | 2026-09-18 | Task 6.3: first placed `db.prepare("SELECT * FROM expenses").all()` at module scope, assigned to `expenses` — ran once at startup, silently stale thereafter. Root-caused a real, confusing symptom (submitted expense survived a plain refresh but *not* a restart-then-refresh) partly independently — correctly distinguished "restart before submit" from "restart after submit" as materially different tests after one nudge — then, when asked precisely, correctly identified the route handler as where a live-per-request query belongs, unaided. Same "when does this code actually run" question as the Section 5 `app.listen`-callback bug, now recognized and generalized to a second context. |
| HTTP methods & REST conventions (GET/POST/DELETE mapping to CRUD) | understood | 2026-09-15 | 2026-09-16 | Explained via the `express.static` experiment (one URL → one response). Task 5.3: wrote a real `app.get("/api/expenses", ...)` route after discussing why `GET` means "read data, don't change anything." Task 5.4: wrote `app.post("/api/expenses", ...)` that creates a resource, deliberately chose to return only the created object with `res.status(201)`. Task 5.6: on the frontend, correctly spotted that two different `fetch` calls in the submit handler were doing different things (one `POST`, one plain `GET`) and identified unprompted which one needed to be awaited to get the created expense back — full `GET`+`POST` round trip now built and working end-to-end, confirmed persisting across a page refresh. |
| Express routing & middleware (`app.get`/`post`/`delete`, `express.json()`) | practicing | 2026-09-15 | 2026-09-18 | First wrote `app.get(express.static("public"))` (wrong — `app.get` with one arg is the settings getter, not a route/middleware registration); after the `app.use` vs `app.get` distinction was explained, independently corrected it to `app.use(express.static("public"))` in the actual file. Also wrote `app.listen(3000, callback)` correctly first try, with a working confirmation log in the callback. 2026-09-16: wrote `app.use(express.json())` correctly unaided (task 5.1); separately tried testing `req.body.amount` inside the `app.listen` callback, hit a `TypeError`, and after the route-handler-vs-lifecycle-callback distinction was explained, correctly removed the test code, predicted and confirmed a clean restart. Then (task 5.3) wrote a full `app.get("/api/expenses", (req, res) => { res.json(expenses); })` route unaided in the right file location, restarted, and confirmed `[]` renders in the browser exactly as predicted. Task 6.3: swapped that same handler's body to `res.json(db.prepare("SELECT * FROM expenses").all())`, correctly predicting the still-empty result *and* the nuanced split between "shows up instantly after submit (rendered straight from the `POST` response)" vs. "gone after a refresh (now reading the empty table via `GET`)" — confirmed live exactly as predicted. |
| `package.json` & npm dependencies | practicing | 2026-09-15 | 2026-09-17 | Added a `"start": "node server.js"` entry to the `scripts` object unaided; correctly predicted `npm start` would behave identically to `node server.js` (just a different invocation, since it's an alias), confirmed by running it — matched exactly, including the "> demo-project@1.0.0 start" preamble npm prints. Task 6.1: predicted `npm install better-sqlite3` would update `package-lock.json`; needed a prompt to recall `package.json`'s `dependencies` also updates, then correctly self-corrected. Initially thought `node_modules/` gets populated "when we run the program" — corrected after discussion that `npm install` downloads the package immediately, and `require()` only ever reads what's already there. `main`, `version` fields still not walked through concept-by-concept. |
| Module system (`require`/`module.exports`) | practicing | 2026-09-15 | 2026-09-17 | Wrote `const express = require("express");`, correctly capturing the returned function into a variable instead of discarding it (previous version was a bare `require("express");` with nothing captured). 2026-09-17 (task 6.1): unaided wrote `const Database = require("better-sqlite3");` followed by `const db = new Database("expenses.db");`, correctly distinguishing "the class I imported" from "the instance I build from it" — also correctly reasoned, unprompted, that only the `require()` line is bound by the imports-at-top convention, not every downstream line that uses an import. `module.exports` side (writing a module that exports something) still not yet exercised. |
| Project folder structure (frontend files vs. backend files) | practicing | 2026-09-13 | 2026-09-14 | Wrote `public/index.html` (structure, form, labels) unaided in the correct location. |
| Same language end-to-end (JS on both frontend & backend) | understood | 2026-09-13 | 2026-09-13 | "JavaScript is the language of the web... to build my fundamentals. That is why I am using it in the backend as well." |

## Engineering practice

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Version control fundamentals (Git commits, history, undo) | understood | 2026-09-13 | 2026-09-13 | Correctly predicted and executed the full init → stage → commit sequence unaided: distinguished staging from committing ("nothing will be committed yet as staging != committing"), predicted a clean `git status` and a populated `git log` after committing, then confirmed both exactly via real output (root-commit `4554660`, "nothing to commit, working tree clean"). |
| Remote repositories (GitHub, push, online backup) | understood | 2026-09-13 | 2026-09-13 | Correctly predicted `git remote add` registers an address without uploading anything, then registered origin, pushed with `git push -u origin main`, and confirmed the files actually appear on github.com/mlathyf/personal-finance-tracker. Also asked and correctly reasoned about how SSH auth differs from HTTPS. |
| `.gitignore` (what not to track) | understood | 2026-09-13 | 2026-09-13 | Wrote `node_modules/` and `.DS_Store` into the file correctly unaided, predicted `git status` would hide `node_modules` but still show `.gitignore` itself, then confirmed with real output matching exactly. |
| Environment variables & secrets (never hardcode passwords) | introduced | 2026-09-13 | 2026-09-13 | Explained as component #7: settings/secrets that must differ from what's committed to GitHub. |
| Dev vs. production environments | introduced | 2026-09-13 | 2026-09-13 | Same explanation as above — laptop vs. live-internet version. |
| Automated testing (writing/running tests) | seed | — | — | — |
| Debugging (reading errors, `console.log`, devtools) | practicing | 2026-09-16 | 2026-09-19 | Ran into a real `TypeError` from testing `req.body` in the wrong callback (5.1), reported the exact stack trace, and correctly diagnosed/fixed it after the concept was explained. Task 5.4 was a richer arc: forgot-to-send-a-response causing a hang, array-vs-object destructuring, and `.push()`'s return value — all found via a pointed question rather than spotted cold. Task 5.6 continued the pattern across three separate bugs in one handler (wrong value passed to `JSON.stringify`/`renderExpense`, a missing render call after removing the old local-only code, two `fetch` calls where only one should exist) — each one still needed a guiding question, but recall of prior lessons (what `e` is, the `await`/`response.json()` pattern) was unprompted and correct each time. Task 6.4: hit a real `TypeError: "...".run is not a function` from a route-handler crash, correctly read and pasted the full stack trace, but predicted "it will do nothing" — wrong, since a thrown, uncaught error actually halts the handler and triggers Express's default 500/HTML error response; needed that propagation chain explained (including why the frontend separately saw `Unexpected token '<'` — parsing the HTML error page as JSON). Still "with help" to *find* subtler bugs and to reason about error propagation; increasingly independent at *fixing* once pointed at, and reads stack traces accurately unaided. |
| Deploying a live app (hosting, persistence tradeoffs) | understood | 2026-09-13 | 2026-09-13 | Chose Render "because it's free and well-documented," explicitly and knowingly deferring the SQLite persistence tradeoff rather than missing it. |
| Code organization conventions (grouping imports, fail-fast rationale) | understood | 2026-09-17 | 2026-09-17 | Task 6.1: after the "group `require()`s at the top" convention was explained along with its rationale (readability, fail-fast on a bad import), asked precisely the right boundary question — whether `const db = new Database(...)` also had to live at the top — and, once told the convention is about imports specifically, correctly reasoned it was fine to place `db` construction below `const app = express()` since neither depends on the other. |

## AI-era practice

| Concept | Status | Introduced | Last reviewed | Evidence |
|---|---|---|---|---|
| Writing a plan before coding (MVP vs. parking lot, scope discipline) | practicing | 2026-09-13 | 2026-09-13 | Actively scoped the MVP and moved the password-gate feature to the parking lot after weighing the tradeoff explicitly, rather than just accepting a list. |
| Persistent agent memory files (files re-read every session) | understood | 2026-09-13 | 2026-09-13 | Requested `project.md` specifically so "every future session will start by reading that file" — stated the purpose unprompted, in their own words. |
| Evaluating tech tradeoffs with an AI pair (recommendation + alternatives + justify before locking) | practicing | 2026-09-13 | 2026-09-19 | Walked through and justified all 5 stack decisions (language, frontend, backend, database, hosting) before locking each in. Task 6.2: presented with the `REAL`-vs-floating-point-rounding tradeoff for storing money, chose integer cents unaided, explicitly citing real-world practice as the reason — then asked a good follow-up (how do you get decimals back out of an integer?) rather than accepting the choice without understanding its mechanics. Task 6.5: hit a real decimal-input bug, self-fixed it by switching `amount` to `type="text"` (works, but loses native numeric validation/keyboard); after the `step="1"` root cause and the tradeoff were explained, chose to revert to `type="number" step="0.01"` instead — the more precise fix — and verified it live. |
| Reviewing a diff / code change before accepting it | practicing | 2026-09-19 | 2026-09-19 | Task 6.4 cleanup step: asked to trace the full file for now-unused code rather than being told what to delete, correctly identified both `expenses` and `nextId` unaided and gave a first-pass reason ("unused now") for each. |
| Living knowledge graph / spaced-review learning tracking | seed | — | — | This file itself — just being seeded. |
| Writing a good task spec when delegating to an AI | seed | — | — | — |
